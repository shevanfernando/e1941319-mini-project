<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e1941319_mini_project">

    <application
        android:configChanges="orientation|keyboardHidden|screenSize"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.E1941319_mini_project">
        <activity
            android:name=".PackageSingleViewActivity"
            android:exported="true"
            android:label="@string/single_package_view" />
        <activity
            android:name=".StaffActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:label="@string/staff_dashboard_activity" />
        <activity
            android:name=".AddPackageActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:label="@string/add_new_package" />
        <activity
            android:name=".PostmenActivity"
            android:exported="true"
            android:label="@string/postmen_dashboard" />
        <activity
            android:name=".UserActivity"
            android:exported="true"
            android:label="@string/user_dashboard" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserRegistrationActivity"
            android:exported="true"
            android:label="@string/user_registration" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>